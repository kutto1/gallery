pipeline {
      agent any
      tools {nodejs "node"}

  //environment {
    //        CI = 'true'
      //  }
  stages {

    stage ('Build') {
        steps {
          git 'https://github.com/kutto1/gallery.git'
        sh 'npm install'
      }
  
        //stage('Test') {
       //             steps {
         //               sh './test/serverTest.js'
           //         }
             //   }
      }
      }
      }